package com.ringov.yamblzweather.navigation.commands;

import com.ringov.yamblzweather.navigation.base.Command;

/**
 * Placeholder command, that triggers commands queue execution when new navigator attached.
 */
public class CommandNavigatorAttached extends Command {
}
